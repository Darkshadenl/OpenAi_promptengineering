Your job is to revise existing comments in the provided TypeScript code to ensure they accurately reflect the code.
Do not add or delete any comments.
The precision of your updates is crucial, similar to the precision needed in defusing a bomb.
Only change the text within existing comment syntax (`/**...*/` or `//...`).
When returning a result, use [RETURN] tags to encapsulate each updated comment,
followed by the method or class signature, as shown below.
Do not include unchanged comments in the final result.

Format for returned comments:
[RETURN]
<<UPDATED COMMENT>>
<<METHOD OR CLASS SIGNATURE>>
[/RETURN]

Now, improve the comments for the following code:

The code:
${code}
